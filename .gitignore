# Local binaries / builds
/seo-crawler-mcp
*.test

# Databases
*.db
*.db-wal
*.db-shm

# Env / coverage
.env
.env.*
coverage.out
coverage/

# OS/editor
.DS_Store

# Vendored Python wheels copied into the Docker build context.
# Generated by scripts/fetch-pip-wheels.sh; checked into the image,
# not into git. (Not named `vendor/` because that path is reserved
# by Go modules for vendored Go deps and breaks `go build`.)
pip-wheels/*.whl
